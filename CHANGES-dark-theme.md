# Тёмная цветовая гамма (аврора) + значок

Перевёл сайт в тёмную «аврора»-палитру присланной страницы: почти чёрный фон #070b14 / #0a1120, светлый текст #e8eef9, мятно-изумрудно-циановый акцент (#5eead4 / #34d399 / #22d3ee).

## Что сделано
1. Центральные токены темы (styles.css :root) переписаны в тёмную аврору: фон, текст, primary (кнопки стали мятными), muted, card, border, hero-градиент, тени. Лендинг подхватывает автоматически, он на токенах.
2. Мои тул- и лендинг-страницы (калькулятор пошлин, de minimis, TikTok, AI-промпты, ROI, тест, шаблоны, скоринг, сравнения, меню языков) прошёл согласованной контекстной заменой: светлый фон в тёмный, тёмный текст в светлый, карточки в тёмные панели, мятный #17B26A в #34d399, #7CE7C2 в #5eead4. Соотношения контраста сохранены. Всего 447 замен по 11 файлам плюс точечные правки иллюстративных секций лендинга (светлая полноширинная секция, чипы, тёмная кнопка навигации, которая иначе слилась бы).
3. Значок (favicon.svg) обновлён под аврору: чистый треугольник с градиентом мята→изумруд→циан. Уже прописан в шапке документа (__root.tsx).
4. Заодно убрал em-тире из события про кредиты (CreditsUpdate) на всех языках, по вашему правилу.

## Важно: нужен ваш визуальный просмотр
Рекраску делал без возможности видеть рендер. Компиляция и типы чистые, соотношения цветов сохранены, но глазами стоит пройтись, особенно по иллюстративным мокапам на главной (фейковые превью дашборда/чата/магазина). Если где-то контраст не идеален, скажите страницу, поправлю точечно.

## Техническое
tsc 0, тире в видимом контенте нет, сборка зелёная.
